o interpretador joga para cima a varivel, isso significa que com `var` podemos inicializar essa variavel, de forma declarativa, isso não significa que um valor sera atribuida a ela.

Veja o exemplo abaixo:

![[Pasted image 20220508105524.png]]
é o mesmo que:
![[Pasted image 20220508105636.png]]

- Hoisting só funciona com `var` se usado com `let` dara erro